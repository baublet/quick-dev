query EnvironmentDetails($id: ID!) {
  environment(input: { id: $id }) {
    id
    name
    subdomain
    ipv4
    lifecycleStatus
    permissions {
      canDelete
    }
    logs {
      startupLogs
      commands {
        id
        title
        status
      }
    }
  }
}
