query EnvironmentDetails($id: ID!) {
  environment(input: { id: $id }) {
    id
    name
    subdomain
    url
    ipv4
    lifecycleStatus
    working
    permissions {
      canDelete
      canOpen
    }
    logs {
      startupLogs
      commands {
        id
        title
        status
      }
    }
  }
}
