query CurrentUserEnvironments {
  user {
    environments {
      totalCount
      nodes {
        deleted
        id
        ipv4
        lifecycleStatus
        name
        size
      }
    }
  }
}
