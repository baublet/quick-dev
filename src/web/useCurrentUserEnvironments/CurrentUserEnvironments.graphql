query CurrentUserEnvironments {
  user {
    id
    environments {
      totalCount
      nodes {
        deleted
        id
        ipv4
        lifecycleStatus
        name
        size
      }
    }
  }
}
